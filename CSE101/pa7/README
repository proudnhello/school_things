Dictionary.cpp implements the Dictionary ADT.  
Dictionary.h defines the Dictionary ADT.  
DictionaryTest.cpp tests the Dictionary ADT.  
To run it, run make DictionaryTest, then ./DictionaryTest.  
  
Order reads in words from an infile, then builds a Binary Search Tree using the lines as keys.  
It then outputs the file sorted into alphabetical order alongside the line numbers, and outputs the BST in pre order.  
To run it, run make, then ./Order <infile> <outfile>